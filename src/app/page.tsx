import HeroSection from "@/components/home/hero-section";
import React from "react";

const Home: React.FC = () => {
  return (
    <section>
      <HeroSection />
    </section>
  );
};

export default Home;
